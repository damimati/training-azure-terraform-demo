# Create storageaccount for terraform statefile storage

/* terraform {
  backend "azurerm" {
    resource_group_name  = "mati_tfstate-rg"
    storage_account_name = "matitfstateacct"
    container_name       = "tfstate"
    key                  = "terraform.tfstate"
  }
}
*/
